import { PropretiesPipe } from './propreties.pipe';

describe('PropretiesPipe', () => {
  it('create an instance', () => {
    const pipe = new PropretiesPipe();
    expect(pipe).toBeTruthy();
  });
});
